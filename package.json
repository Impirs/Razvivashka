{
    "scripts": {
        "vanilla:start": "cd vanilla-app && npm start",
        "vanilla:build": "cd vanilla-app && npm run build",
        "react:dev": "cd react-refactor && npm run dev",
        "react:build": "cd react-refactor && npm run build",
        "react:test": "cd react-refactor && npm run test",
        "pages:dev": "cd pages && npm run dev",
        "pages:build": "cd pages && npm run build"
    }
}